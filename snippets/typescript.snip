snippet     describe
options     head
  describe('${1:NAME}', () => {
    ${2:#:TARGET}
  });

snippet     context
options     head
  context('${1:NAME}', () => {
    ${2:#:TARGET}
  });

snippet     it
options     head
  it('${1:NAME}', () => {
    ${2:#:TARGET}
  });
